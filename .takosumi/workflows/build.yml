version: "0"
jobs:
  - name: build
    steps:
      - name: build-worker
        run: |
          deno task build
          echo "TAKOSUMI_ARTIFACT=sha256:$(sha256sum dist/worker.js | cut -d ' ' -f 1)"
    artifact:
      name: worker
